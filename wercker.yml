box: golang:1.5
build:
    steps:
        - setup-go-workspace
        - script:
            name: build
            code: |
                go get -t ./...
        - script:
            name: test
            code: |
                go test ./...
    after-steps:
        - slack-notifier:
            url: $SLACK_NOTIFIER_URL
            channel: $SLACK_NOTIFIER_CHANNEL
            username: wercker
